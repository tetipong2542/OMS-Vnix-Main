# =============================================================================
# VNIX ERP Environment Configuration
# =============================================================================

# Application Settings
APP_NAME=VNIX ERP
SECRET_KEY=your-secret-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# Option 1: Local Development (SQLite only)
# -----------------------------------------------------------------------------
# Just run the app without any environment variables
# It will use local SQLite files: data.db, price.db, supplier_stock.db

# -----------------------------------------------------------------------------
# Option 2: Production (Turso only)
# -----------------------------------------------------------------------------
# Use Turso cloud database for all data
# TURSO_DATABASE_URL=libsql://your-database-name.turso.io
# TURSO_AUTH_TOKEN=your-auth-token-here
# LOCAL_DB=vnix-erp.db

# -----------------------------------------------------------------------------
# Option 3: DUAL DATABASE MODE (Recommended for Migration)
# -----------------------------------------------------------------------------
# Use both old SQLite databases (read-only) and new Turso database (read/write)
# This mode allows you to:
#  - Keep old data accessible in SQLite (data.db, price.db, supplier_stock.db)
#  - Write all new data to Turso cloud database
#  - Query from both databases simultaneously using dual query helpers

# Enable Dual Database Mode
ENABLE_DUAL_DB_MODE=true

# Turso Database Configuration
TURSO_DATABASE_URL=libsql://vnix-erp-tetipong2542.aws-ap-northeast-1.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token-here

# Local embedded replica file (syncs with Turso)
# This file will be created automatically and synced with Turso
LOCAL_DB=vnix-erp.db

# =============================================================================
# RAILWAY DEPLOYMENT (Optional)
# =============================================================================
# If deploying to Railway with persistent volume
# RAILWAY_VOLUME_MOUNT_PATH=/data

# =============================================================================
# GOOGLE SHEETS INTEGRATION (Optional)
# =============================================================================
# For importing price data from Google Sheets
# GOOGLE_PRIVATE_KEY=your-google-private-key
# GOOGLE_CLIENT_EMAIL=your-service-account-email
# GOOGLE_SHEET_ID=your-google-sheet-id

# =============================================================================
# NOTES
# =============================================================================
#
# DUAL DATABASE MODE Usage:
# 1. Old SQLite databases are accessed as READ-ONLY for historical data
# 2. New Turso database is used for all WRITE operations
# 3. Use db_helpers.py functions for querying both databases:
#    - dual_query_auto(Product) -> returns all products from both databases
#    - dual_query_price(SkuPricing) -> returns prices from both databases
#    - dual_query_supplier(SupplierSkuMaster) -> returns suppliers from both
#
# Database Binds:
# - default: Turso (new data.db equivalent)
# - price: Turso (new price.db equivalent)
# - supplier: Turso (new supplier_stock.db equivalent)
# - data_old: SQLite (old data.db - read only)
# - price_old: SQLite (old price.db - read only)
# - supplier_old: SQLite (old supplier_stock.db - read only)
